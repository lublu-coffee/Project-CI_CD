name: Pull workflow
on:
  workflow_dispatch:
  pull_request:
    types: [opened, reopened, edited]
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: print infa
        run: echo "${{ toJSON(github.event) }}"