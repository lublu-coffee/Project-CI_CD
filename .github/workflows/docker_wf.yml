name: Test App with Docker
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: python:3.12-slim
      ports:
        - 8080:8080
    steps:
      - name: Show Python
        run: python --version
      - name: Show Runner
        run: echo ${{ runner.os }}

